import { sizes } from './sizes';

export const device = {
  xm: `(min-width: ${sizes.xm})`,
  sm: `(min-width: ${sizes.sm})`,
  md: `(min-width: ${sizes.md})`,
  lg: `(min-width: ${sizes.lg})`,
  xl: `(min-width: ${sizes.xl})`,
  '2xl': `(min-width: ${sizes['2xl']})`,
};
